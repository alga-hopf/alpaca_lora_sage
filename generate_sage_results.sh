#!/bin/bash

# Script that takes as input a  prompt from the user and returns the sage results of the output generated by the model.

echo -n "checkpoint name: "
read checkpoint
echo -n "Insert your prompt here: "
read prompt
python3 generate_sage_script.py --prompt $prompt --checkpoint $checkpoint
sage output_code.sage
